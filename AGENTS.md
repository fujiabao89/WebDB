# WebDB Agent 工作协议

## 权威来源与优先级

用户当前指令 > 本文件 > 目录级 `AGENTS.md` > 已接受的 ADR > `webdb-design-draft.md` > 代码注释。发现要求冲突、缺少验收标准或涉及未决架构选择时，停止实施并在任务/PR 中列出冲突；不得自行选择高风险方案。

`webdb-design-draft.md` 是当前产品与 P0 边界的权威设计稿。实施前必须读取与改动相关的章节及 ADR。设计稿迁入 `docs/` 后，应更新本文件中的路径而不是复制设计内容。

## 产品与安全边界

- P0 仅交付 PostgreSQL/MySQL 连接、Schema 拉取、只读 SQL、服务端分页和审计。不要提前实现登录、协作、DML、DDL、生产写入或 SSH 隧道。
- 浏览器不得直连目标数据库，亦不得接收数据库密码、KEK、明文密钥或未脱敏结果。
- SQL 必须由服务端以最小权限账号执行；WebDB RBAC、连接策略和目标数据库原生权限任一层拒绝即拒绝。
- 只读执行必须是单语句、方言 AST 可可靠判定、带超时/行数上限/取消能力；不能可靠判定时默认拒绝。不得用字符串前缀匹配作为安全边界。
- 真实数据库数据不是 CRDT 对象。协作能力只可用于 SQL/笔记等文档内容。
- 不提交密钥、真实用户数据、导出文件、`.env` 或生产日志；测试固定数据必须是合成或脱敏数据。

## 任务协议

1. 读取任务卡、相关 ADR、目录规则和现有代码；先报告目标、非目标、风险与最小计划。
2. 一个分支和 PR 只解决一个 Task。不要顺手重构、升级依赖或扩大 API/Schema。
3. 修改行为前先写或更新会失败的测试；实施最小改动；再运行匹配的验证。
4. 如改动 API、数据模型、权限、SQL 策略、部署、审计或安全边界，必须同时更新相应 ADR/设计/测试。
5. 完成时提供：改动文件、验收标准矩阵、执行命令及原始结果、风险、回滚/前向修复说明和交接摘要。

## 必须升级人工的情况

- 生产连接、DML/DDL、数据导出/删除、权限提升、密钥/身份策略、审计保留策略或外部网络访问。
- 新增或改变 API、Schema、加密方案、SQL 解析器、连接池默认值、依赖许可证。
- 无法可靠解析 SQL、权限/架构规则相互冲突、需要真实凭证、预算耗尽，或同一类失败连续两次。

## Agent Code Review 规则

本节适用于 Codex 及其他代码审查 Agent。审查的目标是发现本次变更引入的、会影响正确性、安全性、数据完整性、兼容性、可靠性或任务验收的问题，并给出可验证证据；不是重新设计功能，也不是代替人工承担合并责任。

### 审查前提与范围

1. 审查前必须读取当前 Task、验收标准、PR diff、适用的所有 `AGENTS.md`、相关 ADR/设计、测试与 CI 结果；不得把 PR 描述、作者声明或已有“已完成”标记当作证据。
2. 以本次 diff 及其直接调用方、被调用方、数据契约和失败路径为主要范围。可以检查相邻代码确认影响，但只报告由本次变更引入或因本次变更而暴露、且本 PR 能合理修复的问题。
3. 逐项核对目标、非目标和验收标准。发现范围漂移、漏实现、实现与契约不一致时，必须指出对应任务条款和代码证据。
4. 根据风险选择验证方式：优先阅读实现和测试，再运行最小可复现命令；不得声称运行过未实际执行的命令。CI 通过只能作为证据之一，不能替代语义审查。
5. 审查任务默认只输出意见，不直接修改代码、提交或推送，也不得在 GitHub 等平台执行批准或合并操作；只有用户明确要求修复时才进入实现流程。审查文本仍须按本节输出 `APPROVE` 等结论，文本结论不等于平台批准。实现 Agent 不得担任自己 PR 的最终独立审查者。

### 必须检查什么

#### 通用正确性

- 正常路径、边界值、空值、错误路径、重试、超时、取消和部分失败是否符合契约。
- 条件判断、状态转换、资源生命周期、并发访问、事务边界和幂等性是否会导致错误结果、竞态、泄漏或重复副作用。
- 调用方与被调用方、前后端、配置、迁移、序列化和持久化模型是否保持兼容；错误码、默认值及字段语义是否一致。
- 是否存在明显的性能退化或资源无界增长，例如 N+1 查询、无界分页/队列/缓存、全量加载、大事务、连接或 goroutine 泄漏。
- 新增行为是否有能在修复前失败、修复后通过的有效测试；测试是否真正触发风险路径，而非只断言 mock 调用或提高覆盖率数字。

#### API、前端与平台

- API 请求/响应、HTTP 状态、错误码、分页和版本兼容性是否符合契约；新增字段、默认值或校验是否会破坏现有调用方。
- 前端变更是否正确处理加载、空数据、错误、重试、取消和重复提交；关键流程是否保留键盘操作、可访问名称和明确反馈。
- 配置、环境变量、容器、健康检查和部署顺序是否有安全默认值，是否会造成启动失败、不兼容滚动发布或无法回滚。
- 日志、指标和 trace 是否足以区分关键失败且不会泄露敏感信息；错误是否被正确传播，而不是静默吞掉或错误标记为成功。

#### 安全、权限与敏感信息

- 所有外部输入是否在正确的信任边界完成验证、规范化和参数绑定；不得依赖客户端校验承担安全职责。
- 跨工作区、跨连接、越权 ID、直接对象引用和导出权限是否被服务端拒绝；WebDB RBAC、连接策略和目标数据库权限是否任一拒绝即拒绝。
- 密钥、密码、连接串、KEK、明文密钥、敏感参数和未脱敏结果是否可能进入代码、配置、API/浏览器响应、日志、审计正文或错误信息。
- 新增依赖、权限、网络访问、配置和部署变更是否扩大攻击面或绕过既有安全边界；许可证变化是否已按规则升级人工。

#### WebDB、SQL 与数据库

- 是否可能通过多语句、注释、MySQL 可执行注释、方言差异、嵌套语句或解析失败绕过 SQL 策略；解析不可靠时是否默认拒绝，且未以字符串前缀匹配充当安全边界。
- SQL 是否由服务端以最小权限账号执行，并使用参数化查询；只读、单语句、超时、行数/字节上限、分页和取消约束是否在服务端强制执行。
- 超时、取消、错误、panic 和客户端断开时，事务是否正确结束，数据库连接是否始终归还，执行/审计状态是否一致。
- Schema/迁移是否有正确约束、索引、外键和回滚/前向修复路径；是否存在锁表、长事务、大表扫描、数据丢失或不兼容部署风险。
- 审计是否追加写，是否记录操作者、工作区、连接、结果摘要与 trace/execution ID；审计失败策略是否符合已接受 ADR，且正文不包含敏感数据。

#### 文档与交付证据

- 行为、API、Schema、权限、SQL 策略、部署、审计或安全边界变化时，相关测试、ADR、设计、任务卡和 README 是否同步且互不冲突。
- 验证命令、CI 状态、迁移说明、风险及回滚/前向修复说明是否与实际变更一致。
- P0 文档必须准确反映当前代码、SQL 迁移、测试和已接受 ADR；不得把后续任务或未实现能力描述为已完成。
- P0 文档需重点核对表结构、约束、索引、凭证字段、追加式审计、数据隔离、过期规则、失效链接和过时路径，并检查是否包含密钥、真实凭证、敏感数据、未脱敏结果或不应公开的日志。

### 不需要检查或不应报告什么

- 不报告本次变更之前已经存在、与当前 diff 无因果关系的问题；如它会直接阻止验收，可单列为“既有阻塞/待确认”，不得冒充本 PR 引入的问题。
- 不把个人风格、命名偏好、空白、排序、注释措辞或可由 formatter/linter 确定处理的事项作为人工审查发现，除非它们造成歧义、错误或违反仓库明确规则。
- 不要求与当前 Task 无关的重构、抽象、依赖升级、功能扩展、测试框架替换或“顺便优化”；不因存在另一种实现方式就判定当前实现有问题。
- 不报告低风险、纯假设且无法给出触发路径和代码证据的泛化风险；此类证据不足项标为“待确认”，说明缺少什么证据，不得据此阻断合并。
- 权限、SQL 安全边界、数据隔离、密钥、生产数据或不可恢复操作等高风险关键事实无法安全确认时，不适用上一条；必须输出 `ESCALATE`、说明缺少的证据并升级人工，不得按低风险“待确认”放行。
- 不仅凭测试覆盖率下降、缺少某类测试、CI 失败或静态工具告警创建意见；必须确认它对应真实行为风险，并避免重复报告同一根因。
- 不审查未被本次变更影响的生成文件、第三方/vendor 代码或大范围历史代码；除非变更错误地生成、引入或依赖了它们。
- 不把 P2/P3 建议夸大为阻断问题，不重复已有未解决对话，不把旧提交上的已修复意见当作当前 HEAD 的新问题。

### 严重级别与合并策略

| 级别 | 判定标准 | 处理要求 |
| --- | --- | --- |
| P0 | 可导致数据泄露、越权、不可恢复的数据破坏、生产大面积中断或安全边界失效 | 必须阻断，立即升级人工 |
| P1 | 当前任务范围内可复现的功能错误、安全/兼容性风险、数据完整性问题或关键性能/可靠性退化 | 必须修复，或由 Owner 记录有期限的例外 |
| P2 | 非关键边界缺失、测试缺口、可维护性或局部性能问题，短期通常不造成核心功能失败 | 本 PR 修复，或创建有 Owner 和期限的后续任务 |
| P3 | 不影响正确性的风格、可读性或可选优化 | 不阻断，仅在团队明确需要时提出 |

严重级别按实际影响与可触发性判定，不按修改文件类型、代码行数或修复成本判定。同一根因只报告一次，并说明所有主要影响位置。

### 每条发现的证据格式

每条审查意见必须同时包含：

1. `[P0-P3]` 简短标题。
2. 精确的文件路径和尽可能小的行号范围；行号应落在本次 diff 上，若根因在相邻代码则说明它如何被本次变更触发。
3. 可验证的事实与触发条件，包括必要的输入、状态或执行顺序。
4. 用户、安全、数据、兼容性、性能或运维影响；避免“可能有问题”一类空泛表述。
5. 复现步骤、失败测试、命令输出、契约/ADR 条款或清晰的代码推理。
6. 最小修复方向，不要求无关重构；无法确认时写明“待确认”和所缺证据。

示例：

```text
[P1] 取消路径未归还数据库连接 — apps/api/query.go:87
事实：ctx 取消后该分支直接 return，未执行 rows.Close，连接池中的连接会保持占用。
触发：请求开始读取结果后客户端断开。
影响：重复触发可耗尽连接池，后续查询超时。
依据：该分支绕过第 102 行的 Close；现有测试未覆盖读取中的取消。
最小修复：将资源关闭注册在成功创建 rows 后，并增加取消场景的连接归还测试。
```

### 审查输出与结论

1. 先列 findings，按 P0 → P3 排序；每条使用上述证据格式。不要用摘要或客套话淹没问题。
2. 再给出验收标准矩阵：`通过 / 失败 / 证据不足`，并列出对应代码、测试或 CI 证据。
3. 最后只能给出以下结论之一（`APPROVE`/`REQUEST CHANGES`/`ESCALATE` 均为审查文本结论，在 GitHub 等平台上不得据此执行批准或合并操作）：
   - `APPROVE`：没有 P0/P1，验收标准有充分证据；如存在 P2，必须已修复或已有明确 Owner 和期限的后续任务，可附不阻断的 P3。
   - `REQUEST CHANGES`：存在已证实的 P0/P1，或关键验收标准失败。
   - `ESCALATE`：规则/ADR 冲突、高风险方案未决、需要真实凭证/生产权限，或关键事实无法由 Agent 安全确认。
4. 结论必须包含置信度和仍未验证的风险。没有发现时明确写“本轮未发现由当前变更引入的可操作问题”，不得用“CI 通过”或“暂时没有新评论”代替无问题结论。

## Git、验证与交接

- 使用 Linear 可识别的短生命周期分支：`feat/WEB-<issue-number>-<slug>`、`fix/WEB-<issue-number>-<slug>` 或 `chore/WEB-<issue-number>-<slug>`。PR 标题必须以同一 Issue ID 开头（例如 `[WEB-14] P0-04 parser spike`），且 PR 正文的 `Task / Issue` 必须填写同一 ID。所有 PR（包括 Draft 与自动化 PR）均须先有真实 Linear 任务；提交继续使用 Conventional Commits。
- 创建 Draft PR 前运行本地可用的格式化、静态检查、测试和构建。不要通过删除/跳过测试使 CI 通过。
- PR Ready 后由独立 Reviewer（优先 Codex）审查；实现 Agent 不能批准或合并自己的 PR。
- P0/P1 审查意见必须修复或由 Owner 在 PR 中记录有期限的例外；P2 必须创建有 Owner 的后续任务；P3 不阻断。
- 上下文切换或阻塞时，在 PR/任务卡记录已读资料、变更、验证、失败尝试、风险和下一步命令。接手 Agent 必须先验证关键事实。
