# Stitch MCP 端点（外部服务，需人工审查后手动启用）
# 使用前请确保已通过 AGENTS.md 要求的外部网络访问审批流程。
# 启用方法：取消下方注释并设置 STITCH_API_KEY 环境变量。
# [mcp_servers.stitch]
# url = "https://stitch.googleapis.com/mcp"
# enabled = true
# default_tools_approval_mode = "writes"
# env_http_headers = { "X-Goog-Api-Key" = "STITCH_API_KEY" }
