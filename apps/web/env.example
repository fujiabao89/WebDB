# WebDB Web 前端配置示例
# 复制为 .env 并按需修改；.env 不会被提交

# API 基础路径。开发和 Compose 默认使用 Vite/nginx 的 /api/v1 代理。
# 只有在同源反向代理不可用时才设置；不得填入 token、凭证或目标数据库地址。
# VITE_API_BASE_URL=http://localhost:8080/api/v1

# 服务端已固定的 Compose 演示工作区 ID。它不是身份或凭证；浏览器不得提交 actor、角色或策略覆盖。
# VITE_WEBDB_WORKSPACE_ID=00000000-0000-0000-0000-000000000000
