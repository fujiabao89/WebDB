# WebDB Web 前端配置示例
# 复制为 .env 并按需修改；.env 不会被提交

# API 服务地址（开发环境通过 Vite proxy 转发）
# API 服务地址 — 仅生产构建使用（Vite proxy 不适用时）
# 开发环境请保持此行注释，使用 Vite proxy (/api → http://api:8080)
# VITE_API_URL=http://localhost:8080
