# ADR-002：v1 仅支持 PostgreSQL 和 MySQL

> 状态：已接受｜日期：2026-07-11

## 背景

多数据库方言、元数据和驱动差异会显著扩大 POC 风险。

## 决策

P0/v1 支持 PostgreSQL 14+ 与 MySQL 8.0+；其他引擎（包括 SQLite 网络连接）另行设计。

## 后果

Adapter 必须隔离引擎差异，并为每项能力维护支持矩阵；不以“通用 SQL”掩盖方言差异。
