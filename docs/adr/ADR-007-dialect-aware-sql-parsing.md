# ADR-007：按方言解析 SQL，未知即拒绝

> 状态：已接受｜日期：2026-07-11

## 背景

字符串前缀或共同 SQL 子集无法可靠阻止多语句、注释和方言绕过。

## 决策

PostgreSQL/MySQL 分别使用对应 AST 解析器；只允许可可靠判定的单条 `SELECT`/`EXPLAIN`，无法判定默认拒绝。

## 后果

解析、分类和拒绝案例必须有两种引擎的回归与模糊测试；DML/DDL 留给独立策略路径。
